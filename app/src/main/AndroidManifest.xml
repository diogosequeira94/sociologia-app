<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sociologiaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NewVideoList.VideoListaIntro"></activity>
        <activity android:name=".NewVideoList.VideoListaTed" />
        <activity android:name=".NewVideoList.VideoListaAutor" />
        <activity android:name=".InfiniteViewPager.ItemAutor" />
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".NewVideoList.VideosLista" />
        <activity android:name=".InfiniteViewPager.MainPager" />
        <activity android:name=".SplashScreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>